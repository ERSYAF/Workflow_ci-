name: movie_recommender

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset_path: {type: str, default: "tmdb_movies_automated_processed.csv"}
    command: "python modelling.py --dataset_path {dataset_path}"
