name: movie-recommender-content-based

entry_points:
  main:
    parameters:
      dataset_path: {type: str, default: "tmdb_movies_processed.csv"}
    command: >
      python Modelling.py --dataset_path {dataset_path}
